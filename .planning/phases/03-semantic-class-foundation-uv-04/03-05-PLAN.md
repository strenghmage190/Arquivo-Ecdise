---
phase: 03-semantic-class-foundation-uv-04
plan: 05
type: execute
wave: 5
depends_on: [03-04]
files_modified:
  - src/components/tools/UVEditor.tsx
autonomous: true
requirements: [UV-04]

must_haves:
  truths:
    - "grep count of `style={{` on UVEditor.tsx drops monotonically from 27 to 11"
    - "All four property groups use `.uv-property-group`; label spacing comes from contextual rules, not element classes (D-01)"
    - "Both hidden-capable file inputs keep their mount identity: :2893 `.uv-hidden` (plan 03-02) and :3075 `.uv-file-input` with `ref={fileInputRef}` — no deduplication (SC#3)"
  artifacts:
    - path: "src/components/tools/UVEditor.tsx"
      provides: "className edits at :3045, :3047, :3051, :3053, :3054, :3055, :3060, :3062, :3063, :3064, :3065, :3073, :3075, :3076, :3077, :3079, :3080, :3081, :3084, :3086"
      contains: "uv-range-slider"
  key_links:
    - from: "classless sliders :3077 (image scale)"
      to: "UVEditor.css `.uv-range-slider:not(.uv-range)` guardrail"
      via: "className=\"uv-range-slider\" — margin-top: 0 preserved (Pitfall 5: 6px base would shift it)"
    - from: "label elements :3054, :3063, :3076"
      to: "UVEditor.css `.uv-property-group input + label` contextual rule"
      via: "no className on the label — spacing arrives from the sibling selector (re-mapped per Pitfall 1)"
---

<objective>
Migrate RESEARCH §Implementation Order steps 8-9: all four generic property groups, the text/color/number/file inputs, the image preview block, and the two classless range sliders.

Purpose: Clear the largest remaining chunk of static styles (16 sites) using D-01 container absorption + contextual sibling rules, and unify all sliders under `.uv-range-slider` (D-05/D-06/D-07).
Output: UVEditor.tsx with 16 more style blocks removed — count 27 → 11.
</objective>

<execution_context>
@D:/Apps/SiteDeInvestigação/.opencode/get-shit-done/workflows/execute-plan.md
@D:/Apps/SiteDeInvestigação/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md — Phase 3 SC#1, SC#2, SC#3 (file pickers)
@.planning/phases/03-semantic-class-foundation-uv-04/03-UI-SPEC.md — §Property groups (+ guardrail: no display:flex), §Inputs & preview (5 rows), §Range sliders (D-05/D-06/D-07 rows + pixel-identical guardrail), Verification Hook 6 (file pickers)
@.planning/phases/03-semantic-class-foundation-uv-04/03-RESEARCH.md — §Style Inventory rows :3045-3086; §Common Pitfalls P1 (dead label+label → input+label), P5 (6px slider-margin trap), P2 (helper-text composition); §Implementation Order steps 8-9
@.planning/phases/03-semantic-class-foundation-uv-04/03-PATTERNS.md — Pattern 5 (contextual descendant/sibling rules), Pattern 9 (slider alias + guardrail)

<interfaces>
<!-- Contracts from plan 03-01's appended CSS block -->
- `.uv-property-group { margin-top: 12px; }` — :3045, :3051, :3060, :3073
- `.uv-property-group input + label { margin-top: 8px; }` — covers :3054, :3063, :3076 (labels get NO className; re-mapped per Pitfall 1)
- `.uv-property-group .uv-flex-row { margin-top: 8px; }` — :3065, :3086 (element carries `uv-flex-row` only)
- `.uv-property-group .uv-image-preview { margin-top: 8px; }` — :3079
- `.uv-text-input { width: 100%; box-sizing: border-box; margin-top: 6px; }` — :3062
- `.uv-number-input { width: 100%; }` — :3064
- `.uv-file-input { display: block; margin-top: 6px; }` — :3075
- `.uv-image-preview { display: flex; flex-direction: column; gap: 6px; }` + `.uv-image-preview img { max-width: 100%; }` — :3079, :3081
- `.uv-helper-text { font-size: 12px; opacity: 0.85; }` + `.uv-helper-text--faint { opacity: 0.8; }` — :3080 composes base + faint; `.uv-helper-text--note { font-size: 13px; opacity: 0.9; margin-top: 8px; }` — :3084 composes base + note (both OUTSIDE .uv-info-box → no line-height applies)
- Sliders: `.uv-range-slider { width: 100%; margin-top: 6px; }`; `.uv-range-slider:not(.uv-range) { margin-top: 0; }` — :3047/:3053/:3055 keep `uv-range` AND gain `uv-range-slider`; :3077 gets `uv-range-slider` only
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Property groups I — brush/softness/text groups + slider aliases (step 8; 27→19)</name>
  <files>src/components/tools/UVEditor.tsx</files>
  <read_first>
    - src/components/tools/UVEditor.tsx:3044-3069 — brush size group, softness/opacity group, text insert group
    - .planning/phases/03-semantic-class-foundation-uv-04/03-RESEARCH.md §Style Inventory rows :3045-3065; §Common Pitfalls P1 (the :3054 label gets NO class — contextual `input + label` covers it)
  </read_first>
  <action>
    In the refactor region ONLY (:2868-3163) — no formatting pass:
    - :3045/:3051/:3060 group divs: remove `style={{marginTop:12}}`, set `className="uv-property-group"` on each.
    - :3047/:3053/:3055 range inputs: change `className="uv-range"` → `className="uv-range uv-range-slider"` (keep the `.uv-range` alias — D-05 — so the 6px margin and full track/thumb look from UVEditor.css :491-502 are retained).
    - :3054 label ("Máscara: Opacidade do Pincel"): remove `style={{marginTop:8}}`, add NO className — the 8px margin arrives via the contextual `.uv-property-group input + label` rule (preceding sibling is the :3053 input; re-mapped per Pitfall 1). Keep the label text byte-identical (copy freeze).
    - :3062 text input: remove `style={{width:'100%',boxSizing:'border-box',marginTop:6}}`, set `className="uv-text-input"`. Keep `type="text"`, value/onChange/placeholder byte-identical.
    - :3063 label ("Tamanho do texto"): remove `style={{marginTop:8}}`, add NO className (contextual rule).
    - :3064 number input: remove `style={{width: '100%'}}`, set `className="uv-number-input"`. Keep `type="number"`, min/max/value/onChange byte-identical.
    - :3065 flex row: remove `style={{display:'flex',gap:8,marginTop:8}}`, set `className="uv-flex-row"` (the 8px top margin comes from `.uv-property-group .uv-flex-row`). Keep the Pronto/Cancelar buttons and handlers byte-identical (no class on them this phase — Phase 5's `.uv-action-btn`).
  </action>
  <verify>
    <automated>(Select-String -Path 'src/components/tools/UVEditor.tsx' -Pattern 'style=\{\{').Count — expect 19; then npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Select-String count of `style=\{\{` on UVEditor.tsx == 19
    - :3045/:3051/:3060 contain `className="uv-property-group"` and no `style=` prop
    - :3047/:3053/:3055 contain `className="uv-range uv-range-slider"` (both classes, in that order)
    - :3054 and :3063 labels contain NO className and no `style=` prop (spacing via contextual `input + label`)
    - :3062 contains `className="uv-text-input"`; :3064 contains `className="uv-number-input"`; :3065 contains `className="uv-flex-row"` — all without `style=`
    - npm run typecheck exits 0
  </acceptance_criteria>
  <done>Three property groups + text/number inputs + slider aliases classed; labels contextual; count 19</done>
</task>

<task type="auto">
  <name>Task 2: Property groups II — image group + preview (step 9; 19→11)</name>
  <files>src/components/tools/UVEditor.tsx</files>
  <read_first>
    - src/components/tools/UVEditor.tsx:3072-3091 — the image insertion group
    - .planning/phases/03-semantic-class-foundation-uv-04/03-RESEARCH.md §Style Inventory rows :3073-3086; §Common Pitfalls P5 (the :3077 slider keeps margin-top 0 via `:not(.uv-range)` guardrail); §Don't Hand-Roll (do NOT deduplicate the :3075 file input)
  </read_first>
  <action>
    In the refactor region ONLY (:2868-3163):
    - :3073 group div: remove `style={{marginTop:12}}`, set `className="uv-property-group"`.
    - :3075 file input: remove `style={{display:'block',marginTop:6}}`, set `className="uv-file-input"`. KEEP `ref={fileInputRef}` (SC#3 — must NOT be deduplicated/merged with the :2893 dock input; mount order preserved), `type="file"`, `accept="image/*"`, and the onChange handler byte-identical.
    - :3076 label ("Escala"): remove `style={{marginTop:8}}`, add NO className (contextual `input + label`).
    - :3077 range input: add `className="uv-range-slider"` (it is classless today; no style to remove). The `.uv-range-slider:not(.uv-range)` guardrail keeps its 0 top margin (Pitfall 5). Keep `type="range"`, min={0.1}, max={3}, step={0.05}, value/onChange byte-identical.
    - :3079 preview wrapper div: remove `style={{marginTop:8}}`, set `className="uv-image-preview"` (8px top margin via `.uv-property-group .uv-image-preview` contextual rule; internal 6px gaps via the class's flex column).
    - :3080 "Pré-visualização" div: remove `style={{fontSize:12,opacity:0.8}}`, set `className="uv-helper-text uv-helper-text--faint"` (base supplies the 12px size; faint supplies opacity 0.8; outside `.uv-info-box` so no line-height applies). Copy frozen.
    - :3081 img: remove `style={{maxWidth:'100%', marginTop:6}}` (max-width via `.uv-image-preview img` descendant rule; the 6px margin replaced by the preview's flex gap). Keep `src={imageEl.src}`, `alt="preview"`, `loading="lazy"` byte-identical.
    - :3084 loading note div (falsy branch): remove `style={{marginTop:8, fontSize:13, opacity:0.9}}`, set `className="uv-helper-text uv-helper-text--note"` (note supplies 13px/0.9/margin-top 8px). Copy frozen — "Carregando imagem... Aguarde antes de clicar no canvas."
    - :3086 flex row: remove `style={{display:'flex',gap:8,marginTop:8}}`, set `className="uv-flex-row"` (8px via contextual rule). Keep Pronto/Cancelar buttons and handlers byte-identical.
  </action>
  <verify>
    <automated>(Select-String -Path 'src/components/tools/UVEditor.tsx' -Pattern 'style=\{\{').Count — expect 11; then npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Select-String count of `style=\{\{` on UVEditor.tsx == 11
    - :3073 contains `className="uv-property-group"`; :3075 contains `className="uv-file-input"` AND `ref={fileInputRef}` — and the file contains exactly TWO `ref={fileInputRef}` occurrences total (:2893 and :3075)
    - :3077 contains `className="uv-range-slider"` (single class, no uv-range); :3079 contains `className="uv-image-preview"`; :3080 contains `className="uv-helper-text uv-helper-text--faint"`; :3084 contains `className="uv-helper-text uv-helper-text--note"`; :3086 contains `className="uv-flex-row"` — all without `style=` (except :3077 which had none)
    - :3081 img has no `style=` prop but retains `src`, `alt="preview"`, `loading="lazy"`
    - npm run typecheck exits 0
  </acceptance_criteria>
  <done>Image group fully classed; both file inputs distinct with refs; sliders unified; count 11</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| className strings → DOM | All values are static literals this plan — no interpolation, no user input |
| file input wiring → browser file picker | `ref={fileInputRef}` still mounted on BOTH inputs; mount order preserved (SC#3) |

ASVS assessment: zero-logic, zero-new-input CSS refactor — V2/V3/V4/V5/V6 all **no** (RESEARCH §Security Domain: existing inputs `:3062` text, `:3064` number, `:3075` file are unchanged in handling; React escapes all rendered values).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-01 | Tampering | label/contextual spacing rules | mitigate | Labels get NO class; spacing via `.uv-property-group input + label` sibling rule (value-identical 8px; dead `label + label` omitted per Pitfall 1); pixel-identity enforced by guard test deviation group |
| T-3-02 | Tampering | slider margin regression | mitigate | `.uv-range-slider:not(.uv-range)` guardrail keeps :3077 at 0 margin (Pitfall 5) — no 6px shift; :3047/:3053/:3055 retain `uv-range` alias so their 6px margin and track/thumb look survive (D-05) |
| T-3-03 | Tampering | file input deduplication | mitigate | Both inputs keep `ref={fileInputRef}` (exactly 2 occurrences asserted); no merge of :2893 and :3075 (SC#3, RESEARCH §Don't Hand-Roll) |
| T-3-SC | Tampering | package installs | accept | Zero packages installed (RESEARCH §Package Legitimacy Audit) |
</threat_model>

<verification>
- Monotonic countdown per task: 27 → 19 → 11
- `npm run typecheck` green after each task
- `grep -c "ref={fileInputRef}"` on UVEditor.tsx == 2 (both inputs intact)
</verification>

<success_criteria>
- Count == 11 after this plan; all four property groups classed; labels spaced contextually; sliders unified under `.uv-range-slider`
- Image preview renders identical: 8px top margin, 6px internal gap, max-width 100% img
- Both file pickers still open (functional; full manual check in plan 03-06)
</success_criteria>

<output>
Create `.planning/phases/03-semantic-class-foundation-uv-04/03-05-SUMMARY.md` when done
</output>
