---
phase: 03-semantic-class-foundation-uv-04
plan: 03
type: execute
wave: 3
depends_on: [03-02]
files_modified:
  - src/components/tools/UVEditor.tsx
autonomous: true
requirements: [UV-04]

must_haves:
  truths:
    - "grep count of `style={{` on UVEditor.tsx drops monotonically from 38 to 33"
    - "Additive aliases in place: uv-workspace/uv-sidebar coexist with load-bearing uv-editor-panel/uv-right-panel (SC#2)"
  artifacts:
    - path: "src/components/tools/UVEditor.tsx"
      provides: "className edits at :2869, :2960, :2969, :2971, :2980, :2981, :2982"
      contains: "uv-workspace"
  key_links:
    - from: ":2869 root div"
      to: "UVEditor.css `.uv-workspace` (empty hook declaration)"
      via: "className=\"uv-editor-panel uv-workspace\" — additive, never substituted"
    - from: ":2969 palette div"
      to: "UVEditor.css `.uv-property-group` + `.uv-property-group--palette`"
      via: "base + variant BOTH required (base fires `.uv-property-group .uv-flex-row` for the color row)"
---

<objective>
Migrate RESEARCH §Implementation Order steps 4-6: additive sidebar aliases (SC#2), palette group (D-01 container absorption + base/variant composition), and the RGB info box.

Purpose: Establish the additive-alias discipline and the property-group spacing model before the high-risk channel-buttons region (next plan).
Output: UVEditor.tsx with 5 more style blocks removed — count 38 → 33.
</objective>

<execution_context>
@D:/Apps/SiteDeInvestigação/.opencode/get-shit-done/workflows/execute-plan.md
@D:/Apps/SiteDeInvestigação/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md — Phase 3 SC#2 (additive aliases, pixel-identical), SC#4
@.planning/phases/03-semantic-class-foundation-uv-04/03-UI-SPEC.md — §Sidebar (`.uv-sidebar`/`.uv-workspace`), §Property groups (base+variant, guardrail: no display:flex), §Info box rows, Verification Hook 5 (byte-identical load-bearing selectors)
@.planning/phases/03-semantic-class-foundation-uv-04/03-RESEARCH.md — §Style Inventory rows :2869, :2960, :2969, :2971, :2980, :2981, :2982; §Pattern 2 (container-absorbed spacing), §Pattern 3 (additive alias); §Implementation Order steps 4-6
@.planning/phases/03-semantic-class-foundation-uv-04/03-PATTERNS.md — Pattern C (additive alias composition), Pattern 5 (contextual rules)

<interfaces>
<!-- Contracts from plan 03-01's appended CSS block -->
- `.uv-workspace {}` / `.uv-sidebar {}` — empty declarations + taxonomy comments (load-bearing rules untouched)
- `.uv-property-group { margin-top: 12px; }` + `.uv-property-group--palette { margin-top: 0; margin-bottom: 12px; }` — :2969 uses BOTH
- `.uv-flex-row { display: flex; gap: 8px; }` + `.uv-color-row { align-items: center; }` — :2971; its 8px top margin comes from `.uv-property-group .uv-flex-row` (contextual, fires because :2969 carries the base class)
- `.uv-info-box { margin-top: 12px; padding: 12px; background: rgb(var(--nexus-blue-rgb) / 3%); border: 1px solid rgb(var(--nexus-blue-rgb) / 12%); border-radius: 8px; }`
- `.uv-info-box-title { font-size: 14px; font-weight: 600; color: var(--nexus-blue); display: block; margin-bottom: 8px; }`
- `.uv-helper-text { font-size: 12px; opacity: 0.85; }` + contextual `.uv-info-box .uv-helper-text { margin-bottom: 12px; line-height: 1.4; }` — :2982 carries ONLY the base class; the 1.4 line-height arrives via the contextual rule
<!-- Guardrail: do NOT switch .uv-property-group to display:flex/gap — .properties-panel label { margin-bottom: 6px } (:791-796) would compound and shift every label→control distance -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sidebar aliases + palette (steps 4-5; 38→36)</name>
  <files>src/components/tools/UVEditor.tsx</files>
  <read_first>
    - src/components/tools/UVEditor.tsx:2868-2878 (root div), :2959-2976 (right panel + palette region)
    - .planning/phases/03-semantic-class-foundation-uv-04/03-RESEARCH.md §Pattern 3 (additive alias — composition, never substitution); §Style Inventory rows :2869, :2960, :2969, :2971
  </read_first>
  <action>
    In the refactor region ONLY (:2868-3163) — no edits above :2868, no formatting pass:
    - :2869 root div: change `className="uv-editor-panel"` → `className="uv-editor-panel uv-workspace"` (additive — the grid container name `uv-editor-panel` MUST stay; keep `ref={rootRef}`).
    - :2960 right panel div: change `className="uv-right-panel"` → `className="uv-right-panel uv-sidebar"` (additive only).
    - :2969 palette group div: remove `style={{marginBottom:12}}`, set `className="uv-property-group uv-property-group--palette"` — base AND variant BOTH required (the base makes the contextual `.uv-property-group .uv-flex-row` fire on the color row below; the variant zeroes the base's margin-top and supplies the original 12px bottom margin).
    - :2971 color row div: remove `style={{display:'flex',alignItems:'center',gap:8,marginTop:8}}`, set `className="uv-flex-row uv-color-row"` (the 8px top margin comes from the contextual rule, not the element).
    - :2972 swatch div: DO NOT TOUCH — locked exception E3 (stays inline with `background: color`).
  </action>
  <verify>
    <automated>(Select-String -Path 'src/components/tools/UVEditor.tsx' -Pattern 'style=\{\{').Count — expect 36; then npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Select-String count of `style=\{\{` on UVEditor.tsx == 36
    - :2869 contains `className="uv-editor-panel uv-workspace"` (both names present) and `ref={rootRef}`
    - :2960 contains `className="uv-right-panel uv-sidebar"` (both names present)
    - :2969 contains `className="uv-property-group uv-property-group--palette"` and no `style=` prop
    - :2971 contains `className="uv-flex-row uv-color-row"` and no `style=` prop
    - :2972 swatch still has its inline `style={{width:28,height:28,borderRadius:6,background:color,border:'1px solid rgba(0,0,0,0.2)'}}`
    - npm run typecheck exits 0
  </acceptance_criteria>
  <done>Aliases additive; palette uses base+variant; color row classed; count 36; swatch exception intact</done>
</task>

<task type="auto">
  <name>Task 2: Info box — RGB channel section (step 6; 36→33)</name>
  <files>src/components/tools/UVEditor.tsx</files>
  <read_first>
    - src/components/tools/UVEditor.tsx:2978-2985 — the info box opening (the section spans to :3041; only :2980-2982 edited this plan)
    - .planning/phases/03-semantic-class-foundation-uv-04/03-RESEARCH.md §Style Inventory rows :2980, :2981, :2982; §Common Pitfalls P2 (.uv-helper-text composition trap)
  </read_first>
  <action>
    In the refactor region ONLY (:2868-3163):
    - :2980 info box container div: remove `style={{marginTop:12, padding:12, background:'rgba(0,215,255,0.03)', border:'1px solid rgba(0,215,255,0.12)', borderRadius:8}}`, set `className="uv-info-box"`.
    - :2981 title label: remove `style={{fontSize:14, fontWeight:600, color:'#00d7ff', display:'block', marginBottom:8}}`, set `className="uv-info-box-title"`. Keep the text `🎯 Canal Alvo RGB` byte-identical (copy freeze).
    - :2982 description div: remove `style={{fontSize:12, opacity:0.85, marginBottom:12, lineHeight:1.4}}`, set `className="uv-helper-text"` — base class ONLY; the 12px bottom margin and line-height: 1.4 arrive via the contextual `.uv-info-box .uv-helper-text` rule (this div is the ONLY element inside `.uv-info-box` carrying `.uv-helper-text` — verified against the region). Keep the description text byte-identical (copy freeze).
    - Do NOT touch :2985-3041 (channel buttons/hint/name — next plan).
  </action>
  <verify>
    <automated>(Select-String -Path 'src/components/tools/UVEditor.tsx' -Pattern 'style=\{\{').Count — expect 33; then npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Select-String count of `style=\{\{` on UVEditor.tsx == 33
    - :2980 contains `className="uv-info-box"` and no `style=` prop
    - :2981 contains `className="uv-info-box-title"` and the literal text `🎯 Canal Alvo RGB`
    - :2982 contains `className="uv-helper-text"` (base ONLY — not `uv-helper-text uv-helper-text--hint`) and no `style=` prop
    - npm run typecheck exits 0
  </acceptance_criteria>
  <done>Info box, title, and description use semantic classes; copy frozen; count 33</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| className strings → DOM | All values are static literals this plan — no interpolation, no user input |

ASVS assessment: zero-logic, zero-new-input CSS refactor — V2/V3/V4/V5/V6 all **no** (RESEARCH §Security Domain). The alias additions do not change grid/panel behavior (empty CSS hooks).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-01 | Tampering | additive alias edits | mitigate | `.uv-workspace`/`.uv-sidebar` have EMPTY declarations — zero styling effect; load-bearing `.uv-editor-panel`/`.uv-right-panel` rules byte-identical (Verification Hook 5); grid `display: grid` unchanged |
| T-3-02 | Tampering | palette base+variant composition | mitigate | Base+variant both present per UI-SPEC; `margin-top: 0` + `margin-bottom: 12px` replicate the removed inline exactly — pixel-identity preserved (no display:flex on `.uv-property-group`; `.properties-panel label` margin cannot compound) |
| T-3-SC | Tampering | package installs | accept | Zero packages installed (RESEARCH §Package Legitimacy Audit) |
</threat_model>

<verification>
- Monotonic countdown per task: 38 → 36 → 33
- `npm run typecheck` green after each task
- `git diff` on UVEditor.tsx limited to :2869, :2960, :2969, :2971, :2980-2982
</verification>

<success_criteria>
- Count == 33 after this plan; aliases verified additive (both class names present per element)
- Info box renders identical: cyan-tinted box (3%/12% alpha via tokens), 14px semibold cyan title, 12px description with line-height 1.4 (contextual rule)
</success_criteria>

<output>
Create `.planning/phases/03-semantic-class-foundation-uv-04/03-03-SUMMARY.md` when done
</output>
