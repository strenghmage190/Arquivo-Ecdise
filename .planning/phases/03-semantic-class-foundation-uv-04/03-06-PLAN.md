---
phase: 03-semantic-class-foundation-uv-04
plan: 06
type: execute
wave: 6
depends_on: [03-05]
files_modified:
  - src/components/tools/UVEditor.tsx
autonomous: false
requirements: [UV-04]

must_haves:
  truths:
    - "grep count of `style={{` on UVEditor.tsx reaches exactly 3 (the documented exceptions: textarea geometry, mask-cursor geometry, swatch backgroundColor) — the 42→3 rule holds"
    - "The jest guard test turns GREEN (count == 3 with live-state-token context per match; zero !important in semantic block; load-bearing selectors present; deviation guard)"
    - "Canvas interaction invariants survive the full migration: mouse AND touch drawing, hidden brush cursor, pan/zoom, both file pickers (SC#3)"
    - "Performance-mode walkthrough passes: editor layout/colors identical to pre-refactor (SC#4)"
  artifacts:
    - path: "src/components/tools/UVEditor.tsx"
      provides: "final className edits at :3126, :3127, :3129, :3134, :3135, :3140, :3141, :3153, :3154"
      contains: "uv-mask-eraser-toggle"
  key_links:
    - from: ":3154 mask hint div"
      to: "UVEditor.css `.uv-flex-row--push`"
      via: "className=\"uv-helper-text uv-flex-row--push\" — push STANDALONE (element is a flex CHILD of :3134, not a flex container; Pitfall 4)"
    - from: "remaining inline styles"
      to: "live-state tokens (inlineTextEdit / maskCursor / brushSize / background: color)"
      via: "lines ~2935, ~2954, ~2972 — the only 3 matches the guard test accepts"
---

<objective>
Complete the UV-04 refactor: migrate the IIFE mask-controls region (step 10), then run the FINAL GATE (step 11): 42→3 countdown complete, jest guard GREEN, typecheck green, UI-SPEC Verification Hooks 1/2/5/7 (automated) — followed by a blocking human walkthrough of the remaining manual hooks 3/4/6 (SC#2/SC#3/SC#4).

Purpose: Finish the migration and prove the ROADMAP success criteria end-to-end.
Output: UVEditor.tsx zero-inline-skeleton with exactly 3 documented exceptions; phase gate evidence.
</objective>

<execution_context>
@D:/Apps/SiteDeInvestigação/.opencode/get-shit-done/workflows/execute-plan.md
@D:/Apps/SiteDeInvestigação/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md — Phase 3 Success Criteria SC#1-SC#4
@.planning/phases/03-semantic-class-foundation-uv-04/03-UI-SPEC.md — §Mask controls rows, §The 3 Inline Exceptions (LOCKED), §Verification Hooks 1-7, §Performance-Mode & Accessibility Contract
@.planning/phases/03-semantic-class-foundation-uv-04/03-RESEARCH.md — §Style Inventory rows :3126-3154; §Common Pitfalls P4 (.uv-flex-row--push standalone), P3 (!important/load-bearing integrity); §Implementation Order steps 10-11; §Validation Architecture (final gate)
@.planning/phases/03-semantic-class-foundation-uv-04/03-PATTERNS.md — Pattern D (IIFE mask-controls region), §Shared Patterns (42→3 grep gate, zero !important, additive alias rule)
@.planning/phases/03-semantic-class-foundation-uv-04/03-VALIDATION.md — §Sampling Rate, §Manual-Only Verifications

<interfaces>
<!-- Contracts from plan 03-01's appended CSS block -->
- `.uv-mask-controls { padding: 12px; border-top: 1px dashed rgb(255 255 255 / 4%); margin-top: 8px; }` — :3126
- `.uv-flex-row { display: flex; gap: 8px; }` + `.uv-mask-controls-header { align-items: center; margin-bottom: 8px; }` — :3127
- `.uv-flex-row` + `.uv-flex-row--push { margin-left: auto; }` — :3129 (Inverter/Limpar wrapper; children :3130-3131 get NO class — Phase 5's `.uv-action-btn`)
- `.uv-flex-row` + `.uv-mask-paint-row { align-items: center; }` — :3134
- `.uv-mask-paint-row label { font-size: 13px; }` — :3135, :3140 (no element class; font-size via descendant rule)
- `.uv-mask-paint-row select + label { margin-left: 12px; }` — :3140 (the 12px left margin)
- `.uv-range-slider uv-range-slider--inline { width: 120px; margin-top: 0; }` — :3141 (inline in the flex row)
- `.uv-mask-eraser-toggle { margin-left: 8px; }` — :3153 (`<button>` — focus-visible already covered by the `:is(button, ...)` list at :61-73)
- `.uv-helper-text` + `.uv-flex-row--push` — :3154 (push STANDALONE — exactly `margin-left: auto`, NO display:flex; element is a flex child of :3134)
<!-- The 3 locked exceptions (must be the ONLY remaining style={{ }}): E1 :2935-2948 textarea (inlineTextEdit.cssX/cssY/fontSize), E2 :2954 mask-cursor (maskCursor.x/y, brushSize), E3 :2972 swatch (background: color) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mask controls — IIFE region (step 10; 11→3)</name>
  <files>src/components/tools/UVEditor.tsx</files>
  <read_first>
    - src/components/tools/UVEditor.tsx:3121-3158 — the full IIFE mask-controls region (including the :3142-3152 eraser-toggle onClick — read-only reference)
    - .planning/phases/03-semantic-class-foundation-uv-04/03-RESEARCH.md §Style Inventory rows :3126-3154; §Common Pitfalls P4 (push standalone); §Implementation Order step 10
  </read_first>
  <action>
    In the refactor region ONLY (:2868-3163) — the IIFE block at :3122-3158; no formatting pass:
    - :3126 container div: remove `style={{padding:12, borderTop:'1px dashed rgba(255,255,255,0.04)', marginTop:8}}`, set `className="uv-mask-controls"`.
    - :3127 header row: remove `style={{display:'flex',gap:8,alignItems:'center',marginBottom:8}}`, set `className="uv-flex-row uv-mask-controls-header"`. Keep `<strong>Editar Máscara</strong>` byte-identical (copy freeze).
    - :3129 Inverter/Limpar wrapper: remove `style={{marginLeft:'auto',display:'flex',gap:8}}`, set `className="uv-flex-row uv-flex-row--push"`. The child buttons Inverter/Limpar (:3130-3131) get NO class this phase — keep their onClick handlers (`invertMask(layer.id)`, `clearMask(layer.id)`) byte-identical.
    - :3134 paint row: remove `style={{display:'flex',gap:8,alignItems:'center'}}`, set `className="uv-flex-row uv-mask-paint-row"`.
    - :3135 label ("Modo pintura:"): remove `style={{fontSize:13}}`, add NO className (`.uv-mask-paint-row label` descendant rule supplies 13px). Copy frozen.
    - :3140 label ("Tamanho"): remove `style={{fontSize:13, marginLeft:12}}`, add NO className (13px via `.uv-mask-paint-row label`; the 12px margin via `.uv-mask-paint-row select + label` sibling rule — preceding sibling is the `<select>`). Copy frozen.
    - :3141 range input: set `className="uv-range-slider uv-range-slider--inline"` (classless today; the inline variant zeroes width→120px and margin-top→0 so the flex row layout holds). Keep `type="range"`, min={1}, max={200}, value/onChange byte-identical.
    - :3153 eraser toggle button: remove `style={{marginLeft:8}}`, set `className="uv-mask-eraser-toggle"`. KEEP the entire onClick handler byte-identical (the prevToolRef/maskUseEraser logic — zero changes).
    - :3154 mask hint div: remove `style={{marginLeft:'auto', fontSize:12, opacity:0.85}}`, set `className="uv-helper-text uv-flex-row--push"` — push is STANDALONE here: this element is a flex CHILD of :3134 being pushed right, NOT a flex container; do NOT add display:flex (Pitfall 4). Copy frozen — "Use pincel/borracha para pintar a máscara".
  </action>
  <verify>
    <automated>(Select-String -Path 'src/components/tools/UVEditor.tsx' -Pattern 'style=\{\{').Count — expect 3; then npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Select-String count of `style=\{\{` on UVEditor.tsx == 3
    - :3126 contains `className="uv-mask-controls"`; :3127 contains `className="uv-flex-row uv-mask-controls-header"`; :3129 contains `className="uv-flex-row uv-flex-row--push"`; :3134 contains `className="uv-flex-row uv-mask-paint-row"` — all without `style=`
    - :3135 and :3140 labels have NO className and no `style=` prop; :3141 contains `className="uv-range-slider uv-range-slider--inline"`; :3153 contains `className="uv-mask-eraser-toggle"` AND the full onClick handler (prevToolRef/maskUseEraser logic) unchanged; :3154 contains `className="uv-helper-text uv-flex-row--push"` and no `style=`
    - The string `display: flex` appears inside `.uv-flex-row--push`'s declaration in UVEditor.css exactly ZERO times (single-declaration rule, Pitfall 4)
    - The only remaining `style=\{\{` matches are the 3 exceptions at ~:2935-2948, ~:2954, ~:2972
    - npm run typecheck exits 0
  </acceptance_criteria>
  <done>Mask controls classed; eraser toggle logic untouched; count == 3; push remains single-declaration</done>
</task>

<task type="auto">
  <name>Task 2: FINAL GATE — 42→3 rule proven, guard test GREEN (step 11)</name>
  <files>src/components/tools/UVEditor.tsx</files>
  <read_first>
    - .planning/phases/03-semantic-class-foundation-uv-04/03-UI-SPEC.md §Verification Hooks 1, 2, 5, 7 (the automated hooks)
    - .planning/phases/03-semantic-class-foundation-uv-04/03-RESEARCH.md §Implementation Order step 11 + §Validation Architecture §Sampling Rate
  </read_first>
  <action>
    Run the full automated phase gate and record the evidence:
    1. Count gate: `(Select-String -Path 'src/components/tools/UVEditor.tsx' -Pattern 'style=\{\{').Count` must return 3, and the 3 matches must be at ~:2935 (textarea — context contains `inlineTextEdit`), ~:2954 (mask-cursor — context contains `maskCursor` and `brushSize`), ~:2972 (swatch — context contains `background: color`). If the count is not 3 or a match lacks its live-state token, STOP and fix the regression before proceeding.
    2. Guard test: `npm test -- --watch=false -- uv04-semantic-classes` must pass GREEN (assertions: count==3 + live-state tokens; block after anchor; zero !important in block; load-bearing selectors; deviation guard input+label present / label+label absent).
    3. Full suite: `npm test` + `npm run typecheck` must both pass.
    4. Verification Hook 2: PowerShell `$c = Get-Content -Raw 'src/components/tools/UVEditor.css'; $c.Substring($c.IndexOf('/* ===== Semantic classes')) -match '!important'` must return False.
    5. Verification Hook 5: `git diff` on UVEditor.css must show ONLY the appended block (load-bearing `.uv-editor-panel` / `.uv-right-panel` / `.uv-range` / `.tool-draw` / `.tool-erase` selectors byte-identical, never renamed); `git diff` on UVEditor.tsx must show NO edits above :2868.
    6. Verification Hook 7: block lives at END of UVEditor.css, sectioned "Semantic classes" with the 9 region comment headers.
    Record all outputs in the plan SUMMARY for the phase auditor.
  </action>
  <verify>
    <automated>npm test -- --watch=false -- uv04-semantic-classes; npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `style=\{\{` count == 3 with each match's context containing its live-state token (inlineTextEdit / maskCursor+brushSize / background: color)
    - `npm test -- --watch=false -- uv04-semantic-classes` exits 0 (GREEN — the guard test flips from RED at creation to GREEN here)
    - `npm test` and `npm run typecheck` both exit 0
    - Zero `!important` in the semantic block (PowerShell check False); UVEditor.css diff is append-only; UVEditor.tsx has no diff above :2868
  </acceptance_criteria>
  <done>Automated phase gate fully GREEN; 42→3 rule proven; guard test flipped from RED to GREEN</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Phase walkthrough — SC#2/SC#3/SC#4 manual verification (Verification Hooks 3, 4, 6)</name>
  <files>src/components/tools/UVEditor.tsx, src/components/tools/UVEditor.css</files>
  <action>This checkpoint closes the phase: the user manually walks through UI-SPEC Verification Hooks 3, 4, 6 (pixel-diff, canvas invariants, performance-mode) against the fully refactored component — no file content changes in this task; user approval is the final phase gate.</action>
  <read_first>
    - .planning/phases/03-semantic-class-foundation-uv-04/03-VALIDATION.md §Manual-Only Verifications (test instructions)
    - .planning/phases/03-semantic-class-foundation-uv-04/03-UI-SPEC.md §Verification Hooks 3, 4, 6
  </read_first>
  <what-built>
    The completed refactor: UVEditor.tsx with zero inline presentation styles (3 documented dynamic exceptions) and the semantic class block appended to UVEditor.css. The automated gate is GREEN; these manual hooks prove pixel-identity, canvas invariants, and performance-mode survival — the parts jest cannot test (CSS mocked via identity-obj-proxy).
  </what-built>
  <how-to-verify>
    Run `npm run dev`, open the app, and open the UVEditor (CreateClueModal → UV Editor). Walk through:
    1. HOOK 4 (pixel-diff SC#2): the 3-column grid layout is intact (tools dock | viewport | right panel); panel backgrounds unchanged; the header title ("🔬 Editor RGB Forense" in RGB mode) renders at 13px/600 weight; Save/Close pseudo-icon buttons (✔/✖) render identical to pre-refactor.
    2. HOOK 6 (canvas invariants SC#3): draw with MOUSE — strokes render; the brush cursor stays hidden while drawing; switch to erase — strokes erase; switch to select — arrow cursor; pan/zoom with wheel still works; draw with TOUCH (or device emulation) — strokes render; open the dock image picker (🖼️) AND the properties "Selecionar imagem" picker — both file dialogs open and images insert.
    3. HOOK 3 (performance-mode SC#4): toggle performance mode (app toggle); editor layout and colors survive (no black-on-transparent editor); channel buttons lose their transition (neutralized by the existing perf block) but keep their colors; slider visual is unchanged from pre-refactor perf-mode.
    4. Spot-check the 6px/8px spacings: "Máscara: Opacidade do Pincel" label keeps its 8px gap below the softness slider (the re-mapped `input + label` rule — deviation Task 2 in plan 03-01); the image "Escala" slider sits flush (0 top margin); mask "Tamanho" slider stays inline in its flex row at 120px.
  </how-to-verify>
  <acceptance_criteria>
    - User confirms the 3-column grid, panel backgrounds, header title, and Save/Close icons are pixel-identical (Hook 4)
    - User confirms mouse + touch drawing, hidden cursor, pan/zoom, and BOTH file pickers work (Hook 6)
    - User confirms performance-mode layout/colors survive with no black-on-transparent editor and no new !important (Hook 3)
    - User confirms the re-mapped :3054 label spacing (8px) and the flush :3077 slider (0px) look correct
  </acceptance_criteria>
  <verify>Human verification per <how-to-verify> above: user walks Hooks 3/4/6 in the running dev server and compares against pre-refactor rendering — no automated check applies to this visual/behavioral gate (jest covers Hooks 1/2/5/7 via the fs guard test)</verify>
  <done>The user approved the full walkthrough; SC#2/SC#3/SC#4 verified and the phase can close</done>
  <resume-signal>Type "approved" to close the phase, or describe any visual/behavioral difference</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| className strings → DOM | Static literals only this plan — no interpolation, no user input |
| final gate assertions → phase sign-off | Automated grep/test evidence gates the phase close |

ASVS assessment: zero-logic, zero-new-input CSS refactor — V2/V3/V4/V5/V6 all **no** (RESEARCH §Security Domain). Security verification hooks for the phase: (a) zero `!important` added in the new block; (b) no `dangerouslySetInnerHTML`/`innerHTML` introduced; (c) the 3 remaining inline blocks derive values from existing state variables only.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-01 | Tampering | eraser-toggle className edit | mitigate | onClick handler (prevToolRef/maskUseEraser state machine) kept byte-identical; only the className changes — no logic surface touched |
| T-3-02 | Tampering | push modifier misuse | mitigate | `.uv-flex-row--push` verified single-declaration (`margin-left: auto` only) — Pitfall 4: :3154 is a flex child, not a container; guard test + acceptance criteria both assert no display:flex in push |
| T-3-03 | Tampering | final-state regression | mitigate | Guard test asserts count==3 WITH live-state-token context per match — a wrong 4th style block cannot pass; load-bearing selector byte-identity asserted; git diff confined to :2868-3163 |
| T-3-SC | Tampering | package installs | accept | Zero packages installed (RESEARCH §Package Legitimacy Audit) |
</threat_model>

<verification>
- Monotonic countdown completed: 11 → 3 (phase total 42 → 3)
- `npm test` (full suite) + `npm run typecheck` GREEN; focused guard test GREEN (RED at creation → GREEN here)
- Blocking human checkpoint (Task 3) approves Hooks 3, 4, 6 before phase close
</verification>

<success_criteria>
- ROADMAP SC#1: `grep 'style=\{\{'` returns exactly the 3 documented exceptions (textarea geometry, mask-cursor geometry, swatch backgroundColor)
- ROADMAP SC#2: pixel-identical rendering confirmed (Hook 4); semantic classes additive — `.uv-editor-panel`/`.uv-right-panel`/`.uv-range`/`.tool-draw`/`.tool-erase` byte-identical
- ROADMAP SC#3: canvas invariants confirmed (Hook 6) — mouse+touch draw, hidden cursor, pan/zoom, both file pickers, `fileInputRef` mount order
- ROADMAP SC#4: performance-mode walkthrough passes (Hook 3); zero `!important` in the new block; focus-visible coverage verified (no :61-73 edit needed)
- All UI-SPEC Verification Hooks 1-7 walked: 1, 2, 5, 7 automated (Task 2) + 3, 4, 6 human (Task 3)
</success_criteria>

<output>
Create `.planning/phases/03-semantic-class-foundation-uv-04/03-06-SUMMARY.md` when done
</output>
