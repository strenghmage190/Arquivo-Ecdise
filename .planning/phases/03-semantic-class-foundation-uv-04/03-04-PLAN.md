---
phase: 03-semantic-class-foundation-uv-04
plan: 04
type: execute
wave: 4
depends_on: [03-03]
files_modified:
  - src/components/tools/UVEditor.tsx
autonomous: false
requirements: [UV-04]

must_haves:
  truths:
    - "grep count of `style={{` on UVEditor.tsx drops from 33 to 27"
    - "Channel buttons show correct active-state colors (red #ff4444 / green #44ff44 with #002200 text / blue #4444ff) with the 1px→2px border growth — pixel-identical to pre-refactor"
  artifacts:
    - path: "src/components/tools/UVEditor.tsx"
      provides: "className swaps at :2985, :2989, :3006, :3023, :3038, :3039"
      contains: "uv-channel-btn--red"
  key_links:
    - from: "channel button className (`is-active` ternary)"
      to: "UVEditor.css `.uv-channel-btn--<color>.is-active` state rules"
      via: "className={`uv-channel-btn uv-channel-btn--red ${targetChannel === 'R' ? 'is-active' : ''}`}"
    - from: ":3038 hint div"
      to: "UVEditor.css `.uv-helper-text--hint`"
      via: "STANDALONE class — no base `.uv-helper-text` (composition trap, Pitfall 2)"
---

<objective>
Migrate RESEARCH §Implementation Order step 7 — the highest-risk visual region: the three RGB channel buttons (D-08, colors 100% in CSS with `.is-active` state) and the channel-name `<strong>` (D-09).

Purpose: State-driven styling moves from inline ternaries to CSS compound selectors, using the in-file template-literal className precedent. Followed by a blocking human-verify checkpoint for pixel-identical active-state colors (the one region where a transcription slip is least likely to be caught by grep).
Output: UVEditor.tsx with 6 more style blocks removed — count 33 → 27.
</objective>

<execution_context>
@D:/Apps/SiteDeInvestigação/.opencode/get-shit-done/workflows/execute-plan.md
@D:/Apps/SiteDeInvestigação/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md — Phase 3 SC#1, SC#2 (pixel-identical)
@.planning/phases/03-semantic-class-foundation-uv-04/03-UI-SPEC.md — §Channel buttons (all 8 rows + guardrail: preserve 1px→2px border growth, green active text #002200), §Channel name (D-09), §Utilities `.uv-helper-text--hint`
@.planning/phases/03-semantic-class-foundation-uv-04/03-RESEARCH.md — §Style Inventory rows :2985, :2989, :3006, :3023, :3038, :3039; §Common Pitfalls P2 (.uv-helper-text composition trap) and P6 (border-grow "fix" + green-active text); §Code Examples (channel button className swap, channel name swap)
@.planning/phases/03-semantic-class-foundation-uv-04/03-PATTERNS.md — Pattern A (template-literal className ternary precedent :2881-2885, :2962-2964)

<interfaces>
<!-- Target className strings (from UI-SPEC §Channel buttons / RESEARCH §Code Examples) -->
- Red button :2989: `uv-channel-btn uv-channel-btn--red ${targetChannel === 'R' ? 'is-active' : ''}`
- Green button :3006: `uv-channel-btn uv-channel-btn--green ${targetChannel === 'G' ? 'is-active' : ''}`
- Blue button :3023: `uv-channel-btn uv-channel-btn--blue ${targetChannel === 'B' ? 'is-active' : ''}`
- Hint :3038: `uv-helper-text--hint` — STANDALONE (no base class)
- Channel name :3039: `uv-channel-name--${targetChannel === 'R' ? 'red' : targetChannel === 'G' ? 'green' : 'blue'}`
- Flex row :2985: `uv-flex-row` (no top margin — today has none)
<!-- CSS state rules they map to (already in UVEditor.css from plan 03-01): `.uv-channel-btn--green.is-active { background: #44ff44; color: #002200; border: 2px solid #44ff44; }` — green ACTIVE TEXT IS #002200, NOT #fff -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Channel buttons + hint + channel name (step 7; 33→27)</name>
  <files>src/components/tools/UVEditor.tsx</files>
  <read_first>
    - src/components/tools/UVEditor.tsx:2985-3040 — the full RGB channel region
    - src/components/tools/UVEditor.tsx:2881-2885 — in-file precedent for template-literal className ternaries
    - .planning/phases/03-semantic-class-foundation-uv-04/03-RESEARCH.md §Code Examples (exact target strings) + §Common Pitfalls P2/P6
  </read_first>
  <action>
    In the refactor region ONLY (:2868-3163) — no edits above :2868, no formatting pass:
    - :2985 flex row div: remove `style={{display:'flex',gap:8}}`, set `className="uv-flex-row"` (NO top margin added — today it has none).
    - :2989/:3006/:3023 channel buttons: remove each entire 39-line inline style block; set className per the <interfaces> target strings. KEEP `type="button"` and the `onClick={() => handleTargetChannelChange('R'|'G'|'B')}` handlers byte-identical (className swap is style editing, not logic — D-08). The 1px→2px border growth on activation is preserved exactly by the CSS — do NOT add padding compensation or box-sizing (Pitfall 6).
    - :3038 hint div: remove `style={{fontSize:11, opacity:0.7, marginTop:8, fontStyle:'italic'}}`, set `className="uv-helper-text--hint"` — STANDALONE, do NOT compose with base `.uv-helper-text` (Pitfall 2: this div sits inside `.uv-info-box`, so composing the base would match `.uv-info-box .uv-helper-text` and gain `line-height: 1.4` + `margin-bottom: 12px` it does not have today → SC#2 box growth). Keep the copy `Canal selecionado:` byte-identical.
    - :3039 channel-name `<strong>`: remove `style={{color: ...}}`, set `className={`uv-channel-name--${targetChannel === 'R' ? 'red' : targetChannel === 'G' ? 'green' : 'blue'}`}` (D-09 — exact ternary order R→red, G→green, else blue; matches the CSS `--red`/`--green`/`--blue` rules). Keep `{targetChannel}` rendering byte-identical.
  </action>
  <verify>
    <automated>(Select-String -Path 'src/components/tools/UVEditor.tsx' -Pattern 'style=\{\{').Count — expect 27; then npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Select-String count of `style=\{\{` on UVEditor.tsx == 27
    - :2989 contains `className={`uv-channel-btn uv-channel-btn--red ${targetChannel === 'R' ? 'is-active' : ''}`}` and `onClick={() => handleTargetChannelChange('R')}`; :3006 and :3023 follow the same shape for G and B
    - :3038 contains `className="uv-helper-text--hint"` — the string `uv-helper-text uv-helper-text--hint` does NOT appear anywhere in the file (no composition)
    - :3039 contains the ternary `uv-channel-name--${targetChannel === 'R' ? 'red' : targetChannel === 'G' ? 'green' : 'blue'}` and no `style=` prop
    - No `style=` remains in the :2985-3040 region
    - npm run typecheck exits 0
  </acceptance_criteria>
  <done>All three channel buttons + hint + channel name use state classes; handlers byte-identical; count 27</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification — channel button active states pixel-identical (SC#2)</name>
  <files>src/components/tools/UVEditor.tsx, src/components/tools/UVEditor.css</files>
  <action>This checkpoint pauses for human visual verification of the channel button active/inactive states rendered by Task 1's CSS classes (SC#2) — no file content changes in this task; the user inspects the running dev server and their approval unlocks the property-group regions (plan 03-05).</action>
  <read_first>
    - The 03-UI-SPEC.md §Channel buttons color rows (the expected visual contract)
  </read_first>
  <what-built>
    The three RGB channel buttons now render their colors entirely from CSS classes (`.uv-channel-btn--red/green/blue` + `.is-active`). This is the highest-risk visual transcription of the phase (state colors, border growth, green-active text #002200), so it gets a dedicated human visual check BEFORE the property-group regions are migrated.
  </what-built>
  <how-to-verify>
    1. Start the dev server (`npm run dev`), open the app, and open the UVEditor in RGB mode (CreateClueModal → UV Editor → mode RGB).
    2. Click each channel button (Red/Green/Blue) and confirm the ACTIVE state matches pre-refactor: red active = background #ff4444, white text, 2px #ff4444 border; green active = background #44ff44, DARK text (#002200 — not white), 2px #44ff44 border; blue active = background #4444ff, white text, 2px #4444ff border.
    3. Confirm the INACTIVE buttons show the translucent tint (10% alpha), colored text (#ff8888/#88ff88/#8888ff), 1px 20%-alpha border.
    4. Confirm the button height does NOT jump when toggling active/inactive beyond the pre-refactor 1px→2px border behavior (no extra padding compensation introduced).
    5. Confirm "Canal selecionado:" hint stays 11px italic at 0.7 opacity, and the channel letter after it is colored (red/green/blue).
  </how-to-verify>
  <acceptance_criteria>
    - User confirms all three active-state colors + border growth match pre-refactor rendering
    - User confirms the green active text is dark (#002200), not white
    - User confirms the :3038 hint has no size/line-height change (11px italic)
  </acceptance_criteria>
  <verify>Human verification per <how-to-verify> above: user clicks each channel button in the running dev server and compares active/inactive colors and border growth against the pre-refactor contract — no automated check applies to this visual gate</verify>
  <done>The user approved the channel button rendering as pixel-identical; the phase proceeds to the property-group regions (plan 03-05)</done>
  <resume-signal>Type "approved" to proceed to the property-group regions (plan 03-05), or describe what looks different</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dynamic className strings → DOM | THIS plan introduces the phase's only dynamic classNames: `targetChannel`-derived ternaries — a fixed 'R'|'G'|'B' union, never user-controlled strings |

ASVS assessment: zero-logic, zero-new-input CSS refactor — V2/V3/V4/V5/V6 all **no**. V5 note (RESEARCH §Security Domain): "className strings are static literals or `targetChannel`-derived ('red'|'green'|'blue') — no user input reaches a className."

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-01 | Tampering | dynamic className interpolation | mitigate | Interpolations are bounded to `'red'|'green'|'blue'` and `'is-active'|''` via ternaries over the fixed `targetChannel` union — no injection vector (RESEARCH §Known Threat Patterns) |
| T-3-02 | Tampering | state-color transcription | mitigate | Human-verify checkpoint (Task 2) gates the exact colors/border values; guard test locks `.uv-channel-btn--green.is-active` presence; green-active `#002200` enforced in acceptance criteria (Pitfall 6) |
| T-3-SC | Tampering | package installs | accept | Zero packages installed (RESEARCH §Package Legitimacy Audit) |
</threat_model>

<verification>
- Monotonic countdown: 33 → 27
- `npm run typecheck` green after Task 1
- Task 2 blocking human checkpoint approves active-state visuals before the phase continues
</verification>

<success_criteria>
- Count == 27 after Task 1; no `style=` remains in :2985-3040
- Channel buttons verified visually: 3 active colors correct (green text #002200), 1px→2px border growth preserved, no layout jump
- Copy freeze holds: "🔴 Red / 🟢 Green / 🔵 Blue / Canal selecionado:" unchanged
</success_criteria>

<output>
Create `.planning/phases/03-semantic-class-foundation-uv-04/03-04-SUMMARY.md` when done
</output>
