---
phase: 03-semantic-class-foundation-uv-04
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - src/components/tools/UVEditor.tsx
autonomous: true
requirements: [UV-04]

must_haves:
  truths:
    - "grep count of `style={{` on UVEditor.tsx drops monotonically from 42 to 38"
    - "Canvas inline style removed; touch-action and cursor behavior fully CSS-keyed (SC#3 invariants preserved)"
  artifacts:
    - path: "src/components/tools/UVEditor.tsx"
      provides: "className edits at :2871, :2874, :2893, :2903, :2913 (refactor region :2868-3163 only)"
      contains: "uv-panel-title"
  key_links:
    - from: "canvas (:2903-2913)"
      to: "UVEditor.css `.uv-canvas` + tool cursor rules"
      via: "className=\"uv-canvas\"; existing root classList toggle tool-draw/tool-erase (:2849-2856) drives cursor"
---

<objective>
Migrate the first three regions of the UV-04 refactor per RESEARCH §Implementation Order steps 1-3: header title/actions, dock hidden file input, and the canvas (D-10 cursor/touch-action re-homing).

Purpose: Start the 42→3 monotonic countdown on the riskiest structural element (canvas) before touching the property panel.
Output: UVEditor.tsx with 4 style blocks removed — count 42 → 38.
</objective>

<execution_context>
@D:/Apps/SiteDeInvestigação/.opencode/get-shit-done/workflows/execute-plan.md
@D:/Apps/SiteDeInvestigação/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md — Phase 3 SC#1 (42→3), SC#2 (additive aliases), SC#3 (canvas invariants)
@.planning/phases/03-semantic-class-foundation-uv-04/03-UI-SPEC.md — §Utilities (`.uv-flex-row`, `.uv-flex-row--push`, `.uv-hidden`), §Header (`.uv-panel-title`), §Viewport/canvas (D-10 rows), Verification Hooks 5-6
@.planning/phases/03-semantic-class-foundation-uv-04/03-RESEARCH.md — §Style Inventory rows :2871, :2874, :2893, :2913; §Pattern 3 (additive alias); §Anti-Patterns (cursor:none !important at :393-398 untouched); §Implementation Order steps 1-3
@.planning/phases/03-semantic-class-foundation-uv-04/03-PATTERNS.md — Pattern B (root classList toggle — read-only), Pattern C (additive alias composition)

<interfaces>
<!-- Contracts the executor consumes from plan 03-01's appended CSS block (verify existence before editing JSX) -->
- `.uv-panel-title { font-weight: 600; }` — font-size NOT set (inherits 13px header)
- `.uv-flex-row { display: flex; gap: 8px; }` + `.uv-flex-row--push { margin-left: auto; }` — compose both at :2874
- `.uv-hidden { display: none; }` — dock picker :2893
- `.uv-canvas { touch-action: none; }` — canvas :2903
- `.uv-editor-viewport canvas { cursor: default; }` / `.uv-editor-panel.tool-draw .uv-editor-viewport canvas { cursor: crosshair; }` / `.uv-editor-panel.tool-erase .uv-editor-viewport canvas { cursor: cell; }` — replace the :2913 cursor ternary
<!-- Pre-existing invariants (do NOT touch): `cursor: none !important` UVEditor.css :393-398; root classList toggle UVEditor.tsx :2849-2856; `display: block` already at UVEditor.css :419-427 -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Header + hidden input (steps 1-2; 42→40)</name>
  <files>src/components/tools/UVEditor.tsx</files>
  <read_first>
    - src/components/tools/UVEditor.tsx:2868-2899 — the header + hidden input region being edited
    - src/components/tools/UVEditor.css — confirm the semantic block from plan 03-01 exists and contains `.uv-panel-title`, `.uv-flex-row`, `.uv-flex-row--push`, `.uv-hidden`
    - .planning/phases/03-semantic-class-foundation-uv-04/03-RESEARCH.md §Style Inventory rows :2871, :2874, :2893
  </read_first>
  <action>
    In the refactor region ONLY (:2868-3163) — no edits above :2868, no handler/prop changes, no formatting pass on the file (line numbers must stay stable for later plans):
    - :2871 mode title div: remove `style={{fontWeight:600}}`, set `className="uv-panel-title"`. Keep the ternary content byte-identical.
    - :2874 header actions div: remove `style={{marginLeft:'auto', display:'flex', gap:8}}`, set `className="uv-flex-row uv-flex-row--push"`. Keep the two child buttons (`btn-save`, `btn-close`) and their onClick handlers byte-identical.
    - :2893 dock hidden file input: remove `style={{ display: 'none' }}`, add `className="uv-hidden"`. KEEP `ref={fileInputRef}`, `type="file"`, `accept="image/*"`, and the onChange handler byte-identical (SC#3 — this input and the :3075 input must NOT be deduplicated or merged).
  </action>
  <verify>
    <automated>(Select-String -Path 'src/components/tools/UVEditor.tsx' -Pattern 'style=\{\{').Count — expect 40; then npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Select-String count of `style=\{\{` on UVEditor.tsx == 40 (down from 42)
    - :2871 div contains `className="uv-panel-title"` and no `style=` prop
    - :2874 div contains `className="uv-flex-row uv-flex-row--push"` and no `style=` prop
    - :2893 input contains `className="uv-hidden"` AND `ref={fileInputRef}` and no `style=` prop
    - npm run typecheck exits 0
  </acceptance_criteria>
  <done>Header title and actions use semantic classes; hidden input keeps fileInputRef; count 40</done>
</task>

<task type="auto">
  <name>Task 2: Canvas — D-10 cursor/touch-action re-homing (step 3; 40→38)</name>
  <files>src/components/tools/UVEditor.tsx</files>
  <read_first>
    - src/components/tools/UVEditor.tsx:2901-2914 — the canvas element; :2849-2856 — the existing tool-draw/tool-erase root classList toggle (READ-ONLY — do not touch)
    - .planning/phases/03-semantic-class-foundation-uv-04/03-RESEARCH.md §Style Inventory row :2913; D-10; §Anti-Patterns ("Touching `.uv-editor-viewport canvas { cursor: none !important }` at :393-398" forbidden)
  </read_first>
  <action>
    - :2903 canvas: add `className="uv-canvas"`. Keep `ref={canvasRef}`, `width={800}`, `height={600}`, and all 6 event handlers (`onPointerDown`, `onPointerMove`, `onPointerUp`, `onPointerCancel`, `onWheel`, `onDoubleClick`) byte-identical.
    - :2913: REMOVE the entire `style={{ touchAction: 'none', cursor: ..., display: 'block' }}` prop. touch-action now lives in `.uv-canvas`; `display: block` already exists at UVEditor.css :419-427; the cursor ternary (`tool === 'draw' ? 'crosshair' : tool === 'erase' ? 'cell' : tool === 'select' ? 'default' : 'default'`) disappears entirely — its behavior moves to the CSS rules `.uv-editor-panel.tool-draw .uv-editor-viewport canvas { cursor: crosshair; }` / `.uv-editor-panel.tool-erase .uv-editor-viewport canvas { cursor: cell; }` / `.uv-editor-viewport canvas { cursor: default; }`, keyed on the existing root classList toggle at :2849-2856 (zero JSX logic change; the tautological `select ? 'default' : 'default'` collapses by CSS).
    - Do NOT touch :2935-2948 (textarea) or :2954 (mask-cursor) — locked exceptions E1/E2.
  </action>
  <verify>
    <automated>(Select-String -Path 'src/components/tools/UVEditor.tsx' -Pattern 'style=\{\{').Count — expect 38; then npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Select-String count of `style=\{\{` on UVEditor.tsx == 38
    - The canvas element at :2903-2914 contains `className="uv-canvas"` and NO `style=` prop; all 6 event handlers still present and unchanged
    - UVEditor.tsx :2849-2856 (tool-draw/tool-erase toggle) and UVEditor.css :393-398 (`cursor: none !important`) are byte-identical (git diff shows no change in those ranges)
    - npm run typecheck exits 0
  </acceptance_criteria>
  <done>Canvas uses .uv-canvas class; inline style removed; cursor behavior fully CSS-keyed; count 38</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| className strings → DOM | All values are static literals this plan (`.uv-panel-title`, `.uv-flex-row`, `.uv-hidden`, `.uv-canvas`) — no interpolation, no user input |

ASVS assessment: zero-logic, zero-new-input CSS refactor — V2/V3/V4/V5/V6 all **no** (RESEARCH §Security Domain). No new input paths; the removed cursor ternary was a tautological fixed-union expression, now CSS.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-01 | Tampering | canvas style removal | mitigate | touch-action re-homed to `.uv-canvas` (same `none` value); `cursor: none !important` at :393-398 untouched → hidden-cursor draw behavior survives; canvas `pointer-events: auto !important` / `z-index: 1100 !important` at :383-385 untouched |
| T-3-02 | Tampering | file input className edit | mitigate | `ref={fileInputRef}` kept on :2893; no deduplication with :3075 (SC#3 — mount-order behavior preserved); handler byte-identical |
| T-3-SC | Tampering | package installs | accept | Zero packages installed (RESEARCH §Package Legitimacy Audit) |
</threat_model>

<verification>
- Monotonic countdown verified per task: 42 → 40 → 38
- `npm run typecheck` green after each task
- `git diff` shows only the 5 edited JSX sites; no changes above :2868
</verification>

<success_criteria>
- Count == 38 after this plan; remaining style sites are exactly: textarea (:2935-2948), mask-cursor (:2954), swatch (:2972), palette (:2969), color row (:2971), info box (:2980-2982), channel buttons (:2989/:3006/:3023), hint (:3038), channel name (:3039), property groups (×4), inputs/labels (:3062-3065), file input (:3075), sliders (:3077), preview (:3079-3081), note (:3084), Pronto/Cancelar (:3086), mask controls (:3126-3154)
- Canvas draws with mouse AND touch after the change (manual spot-check; full walkthrough in plan 03-06)
</success_criteria>

<output>
Create `.planning/phases/03-semantic-class-foundation-uv-04/03-02-SUMMARY.md` when done
</output>
